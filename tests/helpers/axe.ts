// Placeholder for Phase 3 (P2 test #31).
//
// When Phase 3 lands the a11y test, implement:
//   - AxeBuilder wrapper configured for WCAG 2 A/AA tags
//   - Client-side filtering of violations by impact (serious | critical only)
//     — do NOT use `builder.disableRules()`, it's unreliable in @axe-core/playwright 4.11
//   - `exclude` selectors for third-party widgets (OAuth buttons, analytics iframes)
//   - A documented block of known-suppressed rule IDs so the test lands green
//     and suppressions can be removed one by one as the app is fixed
//
// See QA_PLAN.md §2 P2 test #31 for the scope.

export const AXE_PLACEHOLDER = true;
